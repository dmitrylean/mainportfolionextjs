import WorksSection from "@/components/WorksSection";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dmitry Lean | Works",
};

export default function WorksPage() {
  return <WorksSection />;
}
