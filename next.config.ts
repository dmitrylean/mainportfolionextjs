/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "upload.wikimedia.org",
      "www.metal-archives.com",
      "open.spotify.com",
      "music.apple.com",
      "youtube.com",
    ],
  },
};

export default nextConfig;
